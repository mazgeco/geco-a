import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-personas-layout',
  imports: [RouterOutlet],
  templateUrl: './personas-layout.html'
})
export class PersonasLayout {}
